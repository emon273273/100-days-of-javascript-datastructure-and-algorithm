<%-include('./partials/header.ejs')%>
    <%-include('./partials/nav.ejs')%>
    <h1>
        
        <%= title %>
    </h1>
    <h1>Hello this is ejs template</h1>
    <%= new Date().toString()%>


        <% if(post.published){%>
            <h3>Title:<%= post.title %>
            </h3>

            <p>Body: <%= post.body %>
            </p>

            <%} else{%>
                <h1>noting to show</h1>
                <%} %>

                    <ul>
                        <% for (let p of posts){%>

                            <li>
                                <%= p.title%> by <%= p.autor%>
                            </li>

                            <% }%>

                    </ul>


                    
                    <%-include('./partials/footer.ejs')%>